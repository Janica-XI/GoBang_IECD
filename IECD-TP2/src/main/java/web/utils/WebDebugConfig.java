package web.utils;

/**
 * Central configuration for debug logging in the web application.
 * Controls whether debug messages are printed to console across all web utilities.
 */
public class WebDebugConfig {
    
    /**
     * Global debug flag for web application logging.
     * Set to true to enable debug output, false to disable.
     */
    public static final boolean DEBUG = true;
    
}